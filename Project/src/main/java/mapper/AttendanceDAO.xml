<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="dao.AttendanceDAO">

    <insert id="checkin" >
  insert into attendance(userid,attdate,attstarttime) values('m5', to_char (sysdate, 'YYYY-MM-DD'), sysdate )
    </insert>
    
    <update id="checkout">
	update attendance set attendtime = sysdate where userid = 'm5' and to_char (attstarttime, 'YYYY-MM-DD') = to_char (sysdate, 'YYYY-MM-DD')
    </update>
    
      <select id="checkincheck" resultType="String" >
 	select to_char (attstarttime, 'HH24:MI:SS') from attendance where userid = 'm5' and to_char (attdate, 'YYYY-MM-DD') = to_char (sysdate, 'YYYY-MM-DD')
    </select>
    
    <select id="checkoutcheck" resultType="String" >
 	select to_char (attendtime, 'HH24:MI:SS') from attendance where userid = 'm5' and to_char (attdate, 'YYYY-MM-DD') = to_char (sysdate, 'YYYY-MM-DD')
    </select>
    
   
  </mapper>